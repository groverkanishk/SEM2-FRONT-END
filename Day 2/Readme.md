## JS-Day-2
- Async Func: we can move to another task before the previous one finishes its execution
- Sync Func: Code is read and executed line by line

## Questions of Array
1. Itrate all the element and print the elements
2. Add 5 to each number inside the array. Take array length 5.
3. Reverse the array. The element should be numeric.
4. Sort the array in decending order.
5. Find the duplicate element and return the unique array.
